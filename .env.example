# Public frontend URL used by Next.js to build image URLs and API requests
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337

# Optional server-only token if your Strapi instance requires authentication
STRAPI_API_TOKEN=
